# Synthetic examples

No participant-level GARD data are distributed with this repository.

A future public example may contain a small synthetic feature table with the same column schema as the manuscript analysis so that the benchmark, fixed-signature validation, and BOPA code paths can be demonstrated without exposing participant information.

Synthetic examples should not reproduce real ObjectIDs, clinical trajectories, or raw EEG samples.
