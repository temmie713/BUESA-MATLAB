# Configuration

Copy:

`config_paths_template.m` -> `config_paths_local.m`

Then edit the local file for your machine. The local configuration is excluded from git so that private data paths are not committed.
